import { SRoundedContainer } from "components/common/common";
import styled from "styled-components";

export const SUploadReconnectContainer = styled(SRoundedContainer)`
grid-row: 3;
grid-column:3;
box-sizing: border-box;
>button{
  margin: 5px;
}
`